import { ColoredBackDirective } from './colored-back.directive';

describe('ColoredBackDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredBackDirective();
    expect(directive).toBeTruthy();
  });
});
